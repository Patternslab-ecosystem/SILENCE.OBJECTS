{
  "name": "SILENCE SENTINEL",
  "version": "1.0.0",
  "type": "guardian",
  "layers": ["vocabulary", "build", "contracts", "closed-modules"],
  "runtime": {
    "middleware": "@silence/safety/sentinel-middleware",
    "mode": "passive"
  },
  "ci": {
    "workflow": ".github/workflows/sentinel.yml",
    "required": true
  }
}
